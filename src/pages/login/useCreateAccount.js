const useCreateAccount = (username, name, email, password) => {
  console.log(`New account with username: ${username}, name: ${name}, email: ${email}, password: ${password}`);

  return true;
};

export default useCreateAccount;
