import React from 'react';

/**
 * Renders the loadingBar for the application.
 * @returns the loadingBar
 */
const LoadingBar = () => (
  <div className="loading__ellipsis">
    <div />
    <div />
    <div />
    <div />
  </div>
);

export default LoadingBar;
